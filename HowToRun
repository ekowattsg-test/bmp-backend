old script: new the new script below.

npm install -g dotenv-cli
dotenv -e .env -- mvn spring-boot:run

mvn -q -DskipTests compile
mvn clean package -DskipTests
 
# Example .env setting to override JWT timeout (milliseconds)
# SECURITY_JWT_TOKEN_VALIDITY_MS=3600000  # 1 hour



use this instead:
  
dotenv -e .env -- mvn spring-boot:run
mvn -q -DskipTests compile
mvn clean package -DskipTests

run this when flyaway failes:
  
  mvn --% -Dflyway.url=jdbc:postgresql://localhost:5432/bmp -Dflyway.user=bmp -Dflyway.password=bmpback flyway:repair